server:
  port: 8081
spring:
  application:
    name: schedule-notification
  datasource:
    url: jdbc:mysql://localhost:3307/schedule_notification_db?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true
    username: admin
    password: admin
    driver-class-name: com.mysql.cj.jdbc.Driver
  rabbitmq:
    host: localhost
    port: 5672
    username: myuser
    password: secret

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect
  mail:
    host: smtp.gmail.com
    port: 587
    username: ""
    password: ""
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
notification:
  days-before: 2
  scheduler:
    job-interval-ms: 60000
broker:
  exchange:
    name: schedule.direct.exchange
  create-queue:
    name: schedule.create
  update-queue:
    name: schedule.update
  cancel-queue:
    name: schedule.cancel
